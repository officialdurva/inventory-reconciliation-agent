[project]
name = "inventory-reconciliation-agent"
version = "0.1.0"
description = "Reconciles partial stock returns when embedded metadata conflicts with the warehouse system of record."
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest==9.1.1"]
